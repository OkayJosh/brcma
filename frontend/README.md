# BRCMA Frontend (Vite + React + TS)

## Setup
```bash
cd frontend
npm install
npm run dev
```

By default the API URL is `http://localhost:8000`. To override, set:
```bash
echo 'VITE_API_URL="http://localhost:8000"' > .env.local
```

Open http://localhost:5173 in your browser.
